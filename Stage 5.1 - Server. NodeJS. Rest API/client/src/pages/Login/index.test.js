/* eslint-disable no-undef */
describe("authentication", () => {
  it("should return true if login data is true", () => {
    // const values = {
    //   username: "data",
    //   password: "31w31",
    // };
    const isAuth = true;

    expect(isAuth).toBe(true);
  });
});
